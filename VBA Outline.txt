Visual Basic
	If Statement
	Loop
	Select Case
	Lookup
	Usefull Stuff
		Paste Value
		Ctrl + Shift + L (filter)
		Ctrl + Shift + Right
		Ctrl + Shift + Left
		Ctrl + Shift + Down
		Return cell value
		One Row Down
		One Row Up
		One Column Left
		One Column Right
		



		
	Structuring Your Code
1. Define Table (Manually)
	1.1. Define Table Earliest and Latest Cell (UpperLeft and LowerRight)
			Then retrieve the properties. Example (B2:F7)
				Dim T1V1 as string
					T1V1 = b
				Dim T1VL as string
					T1VL = f
				Dim T1O1 as string
					T1O1 = 2
				Dim T1OL as string
					T1OL = 7
					

2. Define Variables / Header Range
	2.1. Then get the Range. Example (B2:F2)
			Used to find (search) based on variable name
				Dim H1 as long
					H1 = Range(B2:F2)
				

# How to select all Values From a single variable in example Range(C3:C7)
		1. Search Variable Cell in Header Range (H1)
				Result = C2
					Column Result				= C
					Row Result					= 2
		2. Retrieve the properties
				C = Column Result				= C
				3 = Row Result + One Row Down	= 2 + 1
				7 = T1OL						= 7
		3. Get (C3:C7)
				C3 = Column Result & (Row Result + One Row Down)
				C7 = Column Result & T1OL
		4. Create a code
		
	
	Reference:
	https://excelmacromastery.com/
				
			



	
	
